1. install and create nestjs project
>npm i -g @nestjs/cli
>nest new project-name


project directory structure

2. module generate
>nest generate module customers

module inside controler generate
>nest g controller customers/controllers/customers

module service generate
>nest g service customers/services/customers